# Check that required columns exist in a data.table

Check that required columns exist in a data.table

## Usage

``` r
check_columns(dt, required, caller = "function")
```

## Arguments

- dt:

  data.table to check

- required:

  Character vector of required column names

- caller:

  Name of calling function for error messages
