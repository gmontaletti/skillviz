% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/temporal.R
\name{compute_skill_ranking_series}
\alias{compute_skill_ranking_series}
\title{Compute skill ranking per profession over time periods}
\usage{
compute_skill_ranking_series(
  competenze,
  time_unit = "year",
  cutoff_date = NULL
)
}
\arguments{
\item{competenze}{A \code{data.table} with columns \code{escoskill_level_3}
(skill label), \code{nome_3} (profession name), and \code{gdate} (date,
either Date or IDate class).}

\item{time_unit}{Character, time unit for
\code{lubridate::floor_date()}. Default: \code{"year"}. Other options:
\code{"quarter"}, \code{"month"}, etc.}

\item{cutoff_date}{Date or character in \code{"YYYY-MM-DD"} format.
Observations on or after this date are excluded. Default: \code{NULL}
(no filtering).}
}
\value{
A \code{data.table} with columns \code{professione} (from \code{nome_3}),
\code{mese} (floored date), \code{skill} (from \code{escoskill_level_3}), \code{N}
(count), and \code{rango} (rank within profession-period, 1 = most
frequent).
}
\description{
Aggregates skill counts by profession and time period, then ranks
skills within each profession-period combination (descending by count).
The time unit controls the granularity of aggregation.
}
\details{
This reproduces the temporal ranking logic where skills are ranked
by frequency within each profession at each time step.
}
