% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/match_professions.R
\name{match_professions_score}
\alias{match_professions_score}
\title{Calcola la classifica delle professioni a partire da un prep}
\usage{
match_professions_score(
  prep,
  skills,
  weights = NULL,
  top_n = 20L,
  skill_labels = NULL
)
}
\arguments{
\item{prep}{Oggetto di classe \code{"match_professions_prep"} prodotto da
\code{\link[=match_professions_prepare]{match_professions_prepare()}}.}

\item{skills}{Vettore character di identificativi ESCO L3
(\code{idescoskill_level_3}). Eventuali \code{NA} e duplicati vengono rimossi.}

\item{weights}{Vettore numerico non negativo di pesi per le competenze
in \code{skills}. Se \code{NULL} (default) tutte le competenze ricevono peso
uniforme (comportamento storico). Se fornito deve avere lunghezza
pari a \code{length(unique(skills[!is.na(skills)]))}. Se ha nomi, questi
sovrascrivono l'allineamento posizionale e vengono usati per
accoppiare il peso alla corrispondente competenza. Tutti i pesi
devono essere finiti e non negativi; non possono essere tutti zero.}

\item{top_n}{Numero intero di professioni da restituire in \code{ranking}
e per le quali emettere la decomposizione \code{per_skill}. Default \code{20L}.}

\item{skill_labels}{\code{data.table} opzionale con almeno le colonne
\code{idescoskill_level_3} e \code{escoskill_level_3} (etichetta leggibile).
Quando fornito, la tabella \code{per_skill} ottiene la colonna
\code{skill_label}. Default \code{NULL}.}
}
\value{
Una \code{list} con i seguenti elementi:
\describe{
\item{\code{ranking}}{\code{data.table} con una riga per professione (al piu'
\code{top_n}), ordinata per \code{distance} crescente. Colonne:
\code{idesco_level_4}, \code{distance} (in \verb{[0, 1]}), \code{score = 1 - distance},
\code{hit_ratio} (massa L2 delle competenze dichiarate che la
professione richiede almeno una volta, normalizzata sulla massa
totale; coincide con la frazione di competenze dichiarate nel
caso non pesato), \code{mean_coverage} (media pesata di
\verb{coverage_\{p,s\}} sulle competenze dichiarate; \code{NA} se
\code{basis != "coverage"}), \code{n_postings}, \code{n_skills_matched}.}
\item{\code{per_skill}}{\code{data.table} in formato lungo (vedi
\code{\link[=match_professions]{match_professions()}} per la descrizione completa delle colonne).}
\item{\code{basis}}{La base usata, per introspezione.}
\item{\code{query}}{Il vettore \code{skills} deduplicato.}
\item{\code{params}}{Lista con \code{top_n}, \code{min_postings},
\code{n_professions_considered}, \code{n_skills_in_universe},
\code{n_skills_resolved}, \code{n_skills_unresolved}.}
}
}
\description{
Scoring step: dato un oggetto preparato da \code{\link[=match_professions_prepare]{match_professions_prepare()}}
e un vettore di competenze dichiarate, calcola la distanza coseno fra
il profilo di ogni professione e la query, ordina le professioni per
distanza crescente e restituisce la decomposizione del contributo di
ciascuna competenza alla distanza totale per le top-N professioni.
}
\details{
La distanza usata e' la distanza coseno (\code{1 - cos(v_p, q)}), equivalente
a meta' della distanza euclidea quadrata fra vettori L2-normalizzati.
Questa scelta e' l'unica metrica della famiglia che fornisce una
decomposizione additiva pulita per singola competenza:
\deqn{1 - \cos(v_p, q) = \frac{1}{2} \sum_s ( \tilde v_{p,s} - \tilde q_s )^2.}
}
\examples{
\dontrun{
con <- itaposts::oja_connect()
on.exit(itaposts::oja_disconnect(con), add = TRUE)

ojv <- skillviz::read_oja_itaposts(con)

prep <- skillviz::match_professions_prepare(
  ojv$postings,
  ojv$skills,
  basis = "coverage"
)

# query non pesata (uniforme)
res_uniform <- skillviz::match_professions_score(
  prep,
  c("S1.4.1", "S4.8.1", "S1.13.2")
)

# query pesata: la prima skill conta il doppio
res_weighted <- skillviz::match_professions_score(
  prep,
  c("S1.4.1", "S4.8.1", "S1.13.2"),
  weights = c(2, 1, 1)
)
}

}
\seealso{
\code{\link[=match_professions_prepare]{match_professions_prepare()}} per la fase di prep,
\code{\link[=match_professions]{match_professions()}} per il wrapper end-to-end.
}
