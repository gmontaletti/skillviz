% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/match_professions.R
\name{match_professions_prepare}
\alias{match_professions_prepare}
\title{Pre-computa la matrice profilo professionale per il matching}
\usage{
match_professions_prepare(
  postings,
  skills_long,
  basis = c("coverage", "tfidf", "rca"),
  min_postings = 30L
)
}
\arguments{
\item{postings}{\code{data.table} con almeno le colonne \code{general_id} e
\code{idesco_level_4}. Tipicamente ottenuto da
\code{itaposts::oja_postings(con) |> dplyr::collect()}.}

\item{skills_long}{\code{data.table} in formato lungo con almeno le colonne
\code{general_id} e \code{idescoskill_level_3}. Una riga per coppia
(annuncio, competenza). Tipicamente ottenuto da
\code{itaposts::oja_skills(con) |> dplyr::collect()}.}

\item{basis}{Base del profilo professionale: \code{"coverage"} (default),
\code{"tfidf"} oppure \code{"rca"}.}

\item{min_postings}{Numero minimo di annunci per professione necessari
per ritenere stabile il profilo. Le professioni sotto soglia vengono
scartate prima del calcolo. Default \code{30L}.}
}
\value{
Una \code{list} con classe \code{"match_professions_prep"} e i seguenti
elementi:
\describe{
\item{\code{M}}{\code{dgCMatrix} sparsa skill x professione con i valori
grezzi del profilo nella base scelta (pre-normalizzazione).}
\item{\code{M_norm}}{\code{dgCMatrix} sparsa con le colonne di \code{M} divise
per la propria norma L2. Le colonne con norma nulla vengono
scartate.}
\item{\code{col_norms}}{Vettore numerico delle norme L2 delle colonne
di \code{M}, con nomi pari a \code{colnames(M)}.}
\item{\code{universe_skills}}{Character con \code{rownames(M)}.}
\item{\code{universe_profs}}{Character con \code{colnames(M)} (post-scarto
delle colonne a norma nulla).}
\item{\code{counts}}{\code{data.table} con \code{idesco_level_4},
\code{idescoskill_level_3}, \code{n_ps}, \code{N_p}, \code{value} (dipendente dalla
base) e \code{coverage_ps} (sempre presente; usata da
\code{\link[=match_professions_score]{match_professions_score()}} per \code{mean_coverage} e \code{hit_ratio}).}
\item{\code{n_per_prof}}{\code{data.table} con \code{idesco_level_4} e \code{N_p}
(numero di annunci per professione, dopo filtro \code{min_postings}).}
\item{\code{basis}}{La base risolta.}
\item{\code{min_postings}}{La soglia applicata.}
}

Se nessuna professione supera \code{min_postings}, oppure se la giunzione
\code{postings}-\code{skills_long} e' vuota, viene restituito un oggetto con la
medesima struttura ma con \code{M}, \code{M_norm}, \code{counts} e \verb{universe_*}
vuoti. \code{\link[=match_professions_score]{match_professions_score()}} gestisce questo caso restituendo
tabelle vuote ma tipizzate.
}
\description{
Prepara l'oggetto riusabile contenente la matrice sparsa
competenze x professioni (\code{M}) e la sua versione L2-normalizzata
(\code{M_norm}), insieme ai conteggi e all'universo di skill e professioni.
L'oggetto puo' essere riutilizzato da \code{\link[=match_professions_score]{match_professions_score()}} su
query diverse senza ripagare il costo della costruzione del profilo,
che dipende solo dalle tabelle \code{postings} e \code{skills_long}.
}
\details{
Per la semantica delle basi (\code{coverage}, \code{tfidf}, \code{rca}) si veda
\code{\link[=match_professions]{match_professions()}}.
}
\examples{
\dontrun{
con  <- itaposts::oja_connect()
on.exit(itaposts::oja_disconnect(con), add = TRUE)

post <- itaposts::oja_postings(con)  |> dplyr::collect() |> data.table::setDT()
skil <- itaposts::oja_skills(con)    |> dplyr::collect() |> data.table::setDT()

# prepara una sola volta, riusa per molte query
prep <- skillviz::match_professions_prepare(
  postings = post,
  skills_long = skil,
  basis = "coverage"
)

res1 <- skillviz::match_professions_score(prep, c("S1.4.1", "S4.8.1"))
res2 <- skillviz::match_professions_score(prep, c("S2.1.3"))
}

}
\seealso{
\code{\link[=match_professions_score]{match_professions_score()}} per il calcolo della distanza a
partire da un prep, \code{\link[=match_professions]{match_professions()}} per il wrapper end-to-end.
}
