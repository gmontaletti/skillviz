% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/relevance.R
\name{compute_balassa_index}
\alias{compute_balassa_index}
\title{Compute the Balassa index (Revealed Comparative Advantage) for skill-profession pairs}
\usage{
compute_balassa_index(skills)
}
\arguments{
\item{skills}{A \code{data.table} with one row per skill occurrence in an announcement.
Required columns: \code{preferredLabel} (profession), \code{escoskill_level_3} (skill).}
}
\value{
A \code{data.table} with columns: \code{preferredLabel}, \code{escoskill_level_3}, \code{N},
\code{tutte_professioni}, \code{tutte_le_skills_della_professione}, \code{tutte_le_ricorrenze},
\code{balassa_index}, and \code{specializzazione} ("specifica" if >= 1, "non specifica"
otherwise).
}
\description{
Measures the degree of specialization of each skill within each profession.
A Balassa index >= 1 indicates the skill is more concentrated in that profession
than across all professions (i.e., it is "specific" to the profession).
}
\details{
The formula for each profession-skill pair is:
\deqn{B = (N_{skill,prof} / N_{prof}) / (N_{skill,all} / N_{all})}
}
