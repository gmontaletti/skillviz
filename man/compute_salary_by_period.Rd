% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/salary.R
\name{compute_salary_by_period}
\alias{compute_salary_by_period}
\title{Compute salary statistics by grouping period}
\usage{
compute_salary_by_period(salary_data, by = c("salary", "mese"))
}
\arguments{
\item{salary_data}{A data.table returned by \code{\link[=extract_salary_data]{extract_salary_data()}}.
Must contain \code{salaryvalue} and the columns listed in \code{by}.}

\item{by}{Character vector of column names to group by. When \code{"mese"}
is included and does not yet exist in \code{salary_data}, it is derived
from the \code{data} column as a year-month string (\code{"YYYY-MM"}).
Defaults to \code{c("salary", "mese")}.}
}
\value{
A data.table with columns from \code{by} plus \code{N} (count),
\code{media} (mean salary) and \code{mediana} (median salary).
}
\description{
Aggregate salary data computing count, mean and median salary per
group. The default grouping produces one row per salary band and
month combination.
}
