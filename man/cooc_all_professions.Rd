% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cooccurrence.R
\name{cooc_all_professions}
\alias{cooc_all_professions}
\title{Compute skill co-occurrence networks for multiple professions}
\usage{
cooc_all_professions(
  skills,
  professions = NULL,
  group_col = "preferredLabel",
  top_n = 30L,
  min_weight = 10L
)
}
\arguments{
\item{skills}{A data.table with at least columns \code{general_id},
\code{escoskill_level_3}, and the column named by \code{group_col}.}

\item{professions}{Optional character vector of profession names to process.
When \code{NULL} (the default), all unique values of \code{group_col} are used.}

\item{group_col}{Character name of the column used to identify professions.
Defaults to \code{"preferredLabel"}.}

\item{top_n}{Integer maximum number of edges per profession (before
largest-component filtering). Defaults to \code{30L}.}

\item{min_weight}{Integer minimum co-occurrence count. Defaults to \code{10L}.}
}
\value{
A data.table with columns \code{professione}, \code{from}, \code{to}, \code{weight}.
Professions producing no valid edges are silently dropped.
}
\description{
Iterates over a set of professions and calls the internal
\code{\link[=.cooc_single]{.cooc_single()}} for each one, binding the results into a single
data.table with a \code{professione} identifier column.
}
\examples{
\dontrun{
skills <- data.table::data.table(
  general_id = c(1, 1, 1, 2, 2, 3, 3, 3),
  escoskill_level_3 = c("A", "B", "C", "A", "B", "A", "B", "C"),
  preferredLabel = rep("Analyst", 8)
)
cooc_all_professions(skills)
}

}
