% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/source_stability.R
\name{compute_source_features}
\alias{compute_source_features}
\title{Compute time-series features per source}
\usage{
compute_source_features(tst)
}
\arguments{
\item{tst}{A tsibble as returned by \code{\link[=prepare_source_tsibble]{prepare_source_tsibble()}},
with column \code{N} and key \code{fonte}.}
}
\value{
A tibble with one row per source containing columns:
\code{fonte}, \code{mea} (mean), \code{sd} (standard deviation), \code{sum} (total),
trend/seasonality strength from \code{\link[feasts:feat_stl]{feasts::feat_stl()}}, and \code{cv}
(coefficient of variation = sd / mean).
}
\description{
Extract STL decomposition features from a tsibble produced by
\code{\link[=prepare_source_tsibble]{prepare_source_tsibble()}}. Returns summary statistics (mean, sd,
sum) together with trend and seasonality strength and the
coefficient of variation.
}
