% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot_skill_ranking}
\alias{plot_skill_ranking}
\title{Plot temporal skill ranking for a profession}
\usage{
plot_skill_ranking(serie, profession, skills = NULL)
}
\arguments{
\item{serie}{A \code{data.table} with columns \code{mese} (date), \code{skill}
(character), \code{rango} (integer rank), \code{professione} (character), and \code{N}
(count). Typically produced by a ranking computation step.}

\item{profession}{Character, the profession label to filter on.}

\item{skills}{Optional character vector of skill labels to include. When
\code{NULL} (the default), the top 10 skills by total count are used.}
}
\value{
A \code{ggplot} object.
}
\description{
Draws a line chart showing how skill ranks evolve over time for a single
profession. Each line represents a skill; the y-axis is the rank position
and the x-axis is the time period.
}
\details{
When \code{skills} is \code{NULL} the function selects the 10 most frequently
occurring skills (summing counts across all time periods).
}
