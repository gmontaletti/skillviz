% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/centroid.R
\name{predict_cp5_centroid}
\alias{predict_cp5_centroid}
\title{Predict CP2021 codes against static skill profiles}
\usage{
predict_cp5_centroid(
  profiles,
  postings,
  skills,
  distance = "cosine",
  prior_weight = 1,
  dense_budget = 2e+08,
  explain = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{profiles}{A \code{cp_profiles} object from \code{\link[=build_cp_profiles]{build_cp_profiles()}}.}

\item{postings}{A data.table of announcements to code. Needs \code{general_id}
and the restrictor column the profiles were built on, plus \code{idsector} when
the profiles carry a sector term. Every row gets exactly one prediction.}

\item{skills}{A data.table with \code{general_id} and the profiles' skill column.}

\item{distance}{Character naming the scoring rule, one of
\code{"multinomial_nb"}, \code{"complement_nb"}, \code{"bernoulli_nb"}, \code{"hellinger"},
\code{"cosine"}, \code{"l2"}, \code{"l1"}, \code{"chisq"}, \code{"jsd"}, \code{"dice"},
\code{"tfidf_cosine"}, \code{"balassa_cosine"}.}

\item{prior_weight}{Numeric multiplier on \verb{log n_c}, the class-size prior
(default 1). \code{0} removes the prior; large values reduce the rule to picking
the largest class, which is the modal cascade.}

\item{dense_budget}{Numeric cap on the elements of a dense query block, used
to chunk large groups (default 2e8).}

\item{explain}{Logical: also return the per-candidate score matrix for each
announcement (default FALSE). Costly; intended for diagnosis.}

\item{verbose}{Logical: print a one-line summary (default TRUE).}
}
\value{
A data.table with \code{general_id}, the profiles' target column,
\code{confidence} and \code{method}. \code{method} is \code{"centroid"} where the model
scored, \code{"single_candidate"} where the group offered only one code,
\code{"frequency"} where the announcement carries no known skill and the class
prior decided alone, and \code{"no_match"} where the group is absent from the
profiles. When \code{explain = TRUE} the result carries a \code{scores} attribute.
}
\description{
Scores each announcement against the profiles of the CP codes observed for
its ESCO group, and returns the best one. No training data is consulted: the
whole model is the object returned by \code{\link[=build_cp_profiles]{build_cp_profiles()}}.
}
\details{
The rule is
\deqn{score(q, c) = \lambda \log n_c + s(q, \theta_c) + \mathrm{sector}}
with \eqn{s} the similarity implied by \code{distance} and \eqn{\theta_c} the
smoothed, group-shrunk profile of candidate \eqn{c}.

\strong{The shrinkage makes the model falsifiable.} As \code{shrink_beta} grows, every
\eqn{\theta_c} in a group collapses onto that group's profile, the similarity
term becomes constant across candidates, and the ranking reduces to
\eqn{\lambda \log n_c} — which is exactly the modal cascade. So a correct
implementation must reproduce the modal baseline in that limit, and
\code{skillviz_workflow/run_cp5_centroid.R} refuses to report any accuracy until
it has checked that it does. Ties are broken on the smaller CP code, matching
the cascade.

Announcements carrying no skill present in the profiles get \code{"frequency"}
rather than \code{"centroid"}: the prior decided them, not the skill vector. That
matches \code{\link[=predict_cp5_knn]{predict_cp5_knn()}}'s convention, which keeps the two models'
model-decided populations comparable.
}
\seealso{
\code{\link[=build_cp_profiles]{build_cp_profiles()}}, \code{\link[=predict_cp5_knn]{predict_cp5_knn()}}
}
