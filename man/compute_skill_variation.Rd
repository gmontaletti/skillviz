% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/temporal.R
\name{compute_skill_variation}
\alias{compute_skill_variation}
\title{Compute year-over-year variation in skill rankings}
\usage{
compute_skill_variation(serie, min_n = 30L, top_k = 10L)
}
\arguments{
\item{serie}{A \code{data.table} as returned by
\code{\link[=compute_skill_ranking_series]{compute_skill_ranking_series()}}, with columns \code{professione},
\code{mese}, \code{skill}, \code{N}, and \code{rango}.}

\item{min_n}{Integer, minimum count in the most recent period for a
skill to be included in the variation analysis. Default: \code{30L}.}

\item{top_k}{Integer, number of top-ranked skills to return per
profession (by most recent rank). Default: \code{10L}.}
}
\value{
A \code{data.table} in wide format with one row per
profession-skill pair. Columns include \code{professione}, \code{skill},
rank and count columns for each time period (named as
\verb{rango_<period>} and \verb{N_<period>}), and \code{variazione} (rank change
from previous to current period).
}
\description{
Takes the output of \code{\link[=compute_skill_ranking_series]{compute_skill_ranking_series()}} and calculates
ranking changes between consecutive time periods. The function
pivots the series to wide format with rank and count columns per
period, filters by a minimum count threshold, computes the rank
variation (previous rank minus current rank, so positive values
indicate improvement), and returns the top-k skills per profession.
}
