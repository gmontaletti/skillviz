% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/relevance.R
\name{compute_idf_classification}
\alias{compute_idf_classification}
\title{Compute IDF classification at the document level}
\usage{
compute_idf_classification(skills_merged, quantile_trim = 0.025)
}
\arguments{
\item{skills_merged}{A \code{data.table} with columns \code{general_id} (document ID)
and \code{escoskill_level_3} (skill label).}

\item{quantile_trim}{Numeric, quantile threshold. Skills with IDF in the
bottom \code{quantile_trim} fraction are classified as highly diffuse ("alta").
Default: 0.025.}
}
\value{
A named \code{list} with:
\describe{
\item{idf}{A \code{data.table} with columns \code{escoskill_level_3}, \code{N},
\code{tf}, \code{idf}, \code{diffusione}.}
\item{threshold_rows}{Integer, number of skills classified as "alta"
(highly diffuse).}
\item{diffuse_skills}{Character vector of skill labels classified as
"alta" diffusion.}
}
}
\description{
Calculates IDF using unique document counts (not aggregated frequencies).
This variant counts the number of distinct documents (announcements) that
mention each skill, providing a document-frequency-based diffusion measure.
}
\details{
The IDF formula is: \eqn{log(1 / (n_{docs\_with\_skill} / n_{docs\_total}))}
which simplifies to \eqn{log(n_{docs\_total} / n_{docs\_with\_skill})}.
}
